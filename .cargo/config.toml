[alias]
fmt-check = "fmt --check"
lint = "clippy --workspace --all-targets -- -D warnings"
test-all = "test --workspace"
test-fast = "test --workspace --lib"
cov-clean = "llvm-cov clean"
cov-lcov = "llvm-cov --lcov --output-path target/lcov.info"
cov-html = "llvm-cov --html --output-dir target/llvm-cov/html"
