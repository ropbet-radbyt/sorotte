# Required-check enforcement, 6 September 2026

Fresh repository API reads confirm that `main` has strict required status checks, administrator enforcement, and no allowed force pushes or deletion. All seven contexts are bound to GitHub Actions App ID `15368`: `verification-required`, `merge-required`, `mutation-required`, `fuzz-required`, `dependency-required`, `package-required` and `native-required`. Required pull-request approvals are zero; this policy does not require a human review.

Enforcement was exercised on disposable branches carrying the same policy. Production `main` remained at `4000eca69b52003b66e81b6998d15c555e7eb6d1`. No production merge or direct update was attempted. The original negative probe, [PR #41](https://github.com/ropbet-radbyt/sorotte/pull/41), retained failed, pending, cancelled and missing-check states. GitHub rejected a direct update of its protected disposable base with `GH006` and rejected its merge with HTTP 405. The original output and cleanup receipt remain under `target/verification/protection-acceptance/`.

[PR #44](https://github.com/ropbet-radbyt/sorotte/pull/44) supplied the completed positive and wrong-revision tests. Its base was implementation candidate `72f93a123144484bff9c341a6bf81626d3155949`; positive head `a06f3459d5bae93a4dd0211744145db034c26beb` added only one harmless evidence document. All seven required checks passed from six original attempt-1 workflow runs. Independent review retained all 25 official artifact ZIPs and checked their content, source, run, attempt and producer identities.

The Rust workflow ran both complete behavior suites, both actual coverage canaries, raw coverage producers, semantic/lifecycle contracts and real minimum-mpv checks. The mutation, fuzz, dependency, package and native workflows produced explicit documentation-only no-op receipts. Those five receipts prove the applicability decision; they are not executed campaigns. The passing probe had no changed Rust lines and does not override the implementation PR's original coverage-classification failure.

After accepting the positive evidence, a second documentation-only commit with `[skip ci]` advanced only the owned probe head to `d1d3f7dfbe1b6776cfce679a644245c4b0d73ec0`. Fresh API observations found no new workflow runs or required checks. Its tree was mergeable, but protection reported it blocked. A single merge request pinned to that exact new SHA returned HTTP 405 and reported that all seven required status checks were expected. This establishes that the passing prior revision did not authorize the new revision. No merge request targeted the passing head.

The raw request, HTTP response, before/after snapshots and independent review are retained under `target/verification/protection-acceptance/final-positive-72f93a12-final-2/`. Authentication failures and stale-SHA conflicts are not accepted as this proof. A premature read-only observation made before the skipped push completed failed its source guard; it remains preserved and supplies no enforcement evidence.

The probe was closed unmerged, and its two exact remote refs, disposable protection, owned worktree and local head were removed. All 2,470 original evidence files, including the 25 official ZIPs, were retained unchanged. The initial cleanup recorder stopped on Windows `os.replace` access denial after closing the PR and removing its protection; the cause remains unclassified. That failed receipt is preserved. A separately reviewed attempt used immutable per-command states, rechecked the partial state and completed only the three remaining removals. The final receipt is `cleanup-preparation-1/cleanup-remainder-execution-1/receipt.json`, SHA-256 `07b25af0557f9e3070e72040aa108eb3a8db77d7cb88417a3ef26ede11badc51`. Independent review rehashed the retained files and verified every removal and unchanged production state; its receipt SHA-256 is `d8b8391b3c24c21d47d7f7d56a6cabb9e86aa81ec3bdc2b09a229bdb5c3fdac6`.

This record keeps the exact probe sources above. The later four-file coverage-classifier/canary repair changes no workflow, selector, required-gate authority, protection reader or policy input. Independent source review found no dependency that changes this enforcement result. A Git input comparison accompanies the committed repair. Its own seven required checks and fresh native positive qualification remain mandatory; neither the probe's Rust results nor historical native results are relabeled for that commit.

The owner installed the protection-reader App and configured the expected Actions variable and secret name. These live policy observations used the existing authenticated GitHub CLI. They do not establish that the App installation token can read Administration data; that remains the separate qualified-main release authorization check described in [the setup guide](../../PROTECTION_READER_SETUP.md). No App credential value was inspected and no release was performed.
